A graph is \emph{Eulerian} if it admits an Euler circuit: a closed walk
traversing each edge exactly once. Under this definition, a graph with no
edges is Eulerian (the empty walk is a valid circuit of length~0), but a
disconnected graph with edges is not.
